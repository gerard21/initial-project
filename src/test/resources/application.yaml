spring:
  application:
    name: initial-project
  datasource:
    url: jdbc:tc:mysql:5.7://localhost/initial-project?useSSL=false&useUnicode=yes&characterEncoding=utf8&allowPublicKeyRetrieval=true
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    locations: classpath:db/migration